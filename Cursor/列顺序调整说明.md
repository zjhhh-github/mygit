# 列顺序调整说明

## 修改日期
2026-01-26

## 修改目标
将**对象(内部备注)**列移到**对象(是否删除)**列前面，与售前通讯录.txt文件保持一致。

## 修改前后对比

### 修改前列顺序
```
对象(昵称) → 对象(微信ID) → 对象(微信号) → 对象(总微信号) → 对象(添加时间) → 对象(是否删除) → 对象(内部备注) → 来源(昵称) → ...
```

### 修改后列顺序
```
对象(昵称) → 对象(微信ID) → 对象(微信号) → 对象(总微信号) → 对象(添加时间) → 对象(内部备注) → 对象(是否删除) → 来源(昵称) → ...
```

## 修改内容

### 1. 列索引常量调整（第104-117行）
```python
# 修改前
COL_OBJ_IS_DELETE = 5         # 对象(是否删除)
COL_OBJ_INTERNAL_NOTE = 6     # 对象(内部备注)

# 修改后
COL_OBJ_INTERNAL_NOTE = 5     # 对象(内部备注)
COL_OBJ_IS_DELETE = 6         # 对象(是否删除)
```

### 2. Treeview列定义调整（第1124行）
```python
# 修改前
columns = ("row_num", "nick", "wxid", "number", "total_number", "time", "is_delete", "obj_internal_note", ...)

# 修改后
columns = ("row_num", "nick", "wxid", "number", "total_number", "time", "obj_internal_note", "is_delete", ...)
```

### 3. Treeview表头和列宽设置（第1133-1161行）
交换了`is_delete`和`obj_internal_note`的heading和column设置顺序。

### 4. Excel导出表头调整（第1479-1484行）
```python
# 修改前
headers = ["对象(昵称)", ..., "对象(添加时间)", "对象(是否删除)", "对象(内部备注)", ...]

# 修改后
headers = ["对象(昵称)", ..., "对象(添加时间)", "对象(内部备注)", "对象(是否删除)", ...]
```

### 5. 数据组装顺序调整（3处）

#### 5.1 AutoAgreeAddFriendTask数据（第2353行）
```python
# 修改前
'values': (nick, wxid_value, number, total_number, formatted_time, is_delete_text, obj_internal_note, ...)

# 修改后
'values': (nick, wxid_value, number, total_number, formatted_time, obj_internal_note, is_delete_text, ...)
```

#### 5.2 ContactAuthMsgTable数据（第2491行）
```python
# 修改前
'values': (nick, wxid_value, number, total_number, formatted_time, is_delete_text, obj_internal_note, ...)

# 修改后
'values': (nick, wxid_value, number, total_number, formatted_time, obj_internal_note, is_delete_text, ...)
```

#### 5.3 售前通讯录.txt数据（第2640行）
```python
# 修改前
'values': (nick, wxid_value, number, total_number, formatted_time, is_delete_text, obj_internal_note, ...)

# 修改后
'values': (nick, wxid_value, number, total_number, formatted_time, obj_internal_note, is_delete_text, ...)
```

### 6. 售前通讯录.txt读取逻辑调整（第2540-2553行）
```python
# 修改前
formatted_time = parts[4].strip()
is_delete_symbol = parts[5].strip()
obj_internal_note_from_file = parts[6].strip()

# 修改后
formatted_time = parts[4].strip()
obj_internal_note_from_file = parts[5].strip()
is_delete_symbol = parts[6].strip()
```

### 7. 文档注释更新（第17-21行）
更新了数据结构说明中的列顺序描述。

## 影响范围分析

### ✅ 不受影响的功能
1. **去重逻辑**：去重基于`(对象微信ID, 来源微信ID)`，使用的是`COL_OBJ_WXID`（索引1）和`COL_SOURCE_WXID`（索引8），这两个索引值未变化。
2. **搜索功能**：搜索遍历所有列，不依赖特定列顺序。
3. **导出功能**：使用列索引常量访问数据，自动适配新顺序。
4. **报名情况查询**：使用`COL_OBJ_INTERNAL_NOTE`常量访问，自动适配新索引。

### ✅ 已同步更新的部分
1. 列索引常量定义
2. Treeview界面显示
3. Excel导出表头
4. 三处数据源的数据组装逻辑
5. 售前通讯录.txt文件读取逻辑
6. 所有相关注释

## 测试建议

### 1. 数据加载测试
- 启动程序，验证数据正常加载
- 检查"对象(内部备注)"和"对象(是否删除)"列位置是否正确

### 2. 去重测试
- 验证AutoAgreeAddFriendTask、ContactAuthMsgTable、售前通讯录.txt三个数据源的去重逻辑正常工作
- 检查数据总数是否符合预期

### 3. 导出测试
- 导出Excel文件
- 验证表头顺序正确
- 验证数据内容与界面显示一致

### 4. 搜索测试
- 搜索"对象(内部备注)"中的内容
- 搜索"对象(是否删除)"中的内容
- 验证搜索结果正确

### 5. 报名情况查询测试
- 执行报名情况查询
- 验证"对象(内部备注)"列数据正确读取

## 风险评估

### 低风险 ✅
- 所有修改都是机械性的列顺序调整
- 使用列索引常量的代码自动适配新顺序
- 去重逻辑完全不受影响
- 无业务逻辑变更

### 注意事项
1. 确保售前通讯录.txt文件已按新顺序调整
2. 首次运行时建议备份数据库
3. 建议在测试环境先验证功能正常

## 代码质量检查

### Linter检查
✅ 无linter错误

### 代码规范
✅ 所有修改遵循现有代码风格
✅ 注释完整清晰
✅ 使用常量避免硬编码

## 总结

本次修改成功将"对象(内部备注)"列移到"对象(是否删除)"列前面，与售前通讯录.txt文件保持一致。修改范围明确，影响可控，不影响任何业务逻辑和去重功能。所有相关代码和注释已同步更新。
